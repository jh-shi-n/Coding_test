# MAX
SELECT MAX(DATETIME)
FROM ANIMAL_INS

# Min
SELECT MIN(DATETIME)
FROM ANIMAL_INS

#Count
SELECT COUNT(ANIMAL_ID)
FROM ANIMAL_INS

#중복제거
SELECT COUNT(DISTINCT(NAME)) as 'count'
FROM ANIMAL_INS
WHERE NAME IS NOT NULL
